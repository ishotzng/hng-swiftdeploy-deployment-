services:
  image: swift-deploy-1-node:latest
  port: 3000
  mode: stable
  version: 1.0.0
  restart_policy: unless-stopped
nginx:
  image: nginx:latest
  port: 8080
  proxy_timeout: 10
  contact: support@swiftdeploy.local
network:
  name: swiftdeploy-net
  driver_type: bridge
